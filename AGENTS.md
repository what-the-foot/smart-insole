# AGENTS.md — 스마트 인솔 공통 규칙

## 프로젝트 목표

이 저장소는 양발 스마트 인솔의 족압 데이터를 수집·저장·시각화하고, 규칙 기반으로 보행 패턴을 분석하여 운동 가이드를 제공하는 MVP를 구현한다.

이 서비스는 의료 진단 시스템이 아니다. 결과는 질환을 확정하지 않고 관찰된 패턴, 데이터 품질, 재측정 또는 전문가 상담 필요성을 설명한다.

## 작업 전 필수 확인

1. `docs/00_PROJECT_BRIEF.md`
2. `docs/01_ARCHITECTURE.md`
3. `docs/09_DECISION_LOG.md`
4. `docs/02_API_AND_REALTIME_CONTRACT.md`
5. 현재 작업의 구현 계획서
6. `docs/07_TASK_BOARD.md`
7. 수정 경로에서 가장 가까운 `AGENTS.md`
8. API·실시간 변경이면 `contracts/`

## 저장소 영역

- `backend/`: Spring Boot API, DB, 분석, 인증, WebSocket
- `frontend/`: React 웹 애플리케이션
- `contracts/`: 백엔드·프론트 공통 계약
- `fixtures/`: 하드웨어 없이 재현하는 입력
- `docs/`: 설계와 완료 기준
- `prompts/`: Codex 단계별 프롬프트
- `scripts/`: 검증·Mock 전송 자동화

## 핵심 도메인 규칙

- 기본 센서 수는 한 발당 8개이며 기기 설정으로 6개도 허용한다.
- 원본 샘플링은 한 발당 100Hz다.
- BLE 수신기는 약 100~200ms 동안 프레임을 모아 백엔드로 보낸다.
- 프론트 실시간 표시는 일반적으로 10~20Hz로 낮춘다.
- 실제 시간 정렬은 `deviceTimeMs`와 `sequence`를 사용한다. `receivedAt`은 운영 추적용이다.
- ADC 원본값은 수정하거나 보정값으로 덮어쓰지 않는다.
- `(sessionId, deviceId, sequence)` 기준 멱등성을 보장한다.
- 왼발·오른발 데이터는 서로 다른 시점에 도착할 수 있다.
- 계약에는 `schemaVersion`, 분석에는 `algorithmVersion`, 센서 위치에는 `sensorLayoutVersion`을 둔다.
- 서버 시각은 UTC `Instant` 또는 ISO-8601 UTC 문자열을 사용한다.
- “평발입니다”, “족저근막염입니다”처럼 진단을 확정하는 표현을 사용하지 않는다.

## 변경 원칙

- 한 작업에서 지정된 작업 ID 하나만 구현한다.
- 관련 없는 리팩터링, 파일 이동, 기술 스택 교체를 하지 않는다.
- 기존 코드와 설정이 있으면 먼저 읽고 재사용한다.
- 새 운영 의존성을 추가하기 전 필요성을 설명한다.
- 계약 변경은 구현보다 먼저 `contracts/`와 문서에 반영한다.
- 문서와 구현이 다르면 임의로 선택하지 말고 차이를 보고한다.
- 테스트를 통과시키기 위해 검증을 약화하거나 기능을 끄지 않는다.
- 테스트를 삭제하거나 항상 성공하는 테스트로 바꾸지 않는다.
- 요청받지 않은 commit, push, merge, 브랜치 삭제를 하지 않는다.

## 작업 흐름

1. 현재 코드와 관련 파일을 탐색한다.
2. 문서·계약과 현재 상태의 차이를 확인한다.
3. 수정 파일, 계약/DB 영향, 테스트를 짧게 계획한다.
4. 최소 완성 단위로 구현한다.
5. 정상·실패·경계 테스트를 작성한다.
6. lint/check/test/build를 실제로 실행한다.
7. diff를 리뷰해 범위 초과와 회귀를 확인한다.
8. 계약·문서·작업 보드를 필요한 만큼 갱신한다.
9. 실행한 명령과 실제 결과, 남은 위험을 보고한다.

## 공통 검증 명령

저장소의 실제 스크립트와 잠금 파일을 우선한다.

```bash
cd backend
./gradlew test
./gradlew check

cd ../frontend
npm run lint
npm run test -- --run
npm run build

cd ..
./scripts/verify-all.sh
```

환경 제약으로 실행하지 못했다면 이유와 미검증 항목을 명확히 보고한다. 성공했다고 추정하지 않는다.

## 보안

- 비밀번호 평문 저장·로그 금지
- JWT Secret, DB 비밀번호, Receiver API Key 커밋 금지
- `.env`, 빌드 결과, 로컬 파일은 `.gitignore` 확인
- 사용자 ID를 요청 본문에서 신뢰하지 않고 인증 주체에서 얻는다.
- 타인의 기기, 세션, 결과, WebSocket topic 접근을 막는다.
- Receiver 내부 API는 사용자 JWT와 별도 인증을 사용한다.
- 센서 원본 전체를 일반 로그에 출력하지 않는다.

## 완료 보고 형식

1. 작업 ID
2. 구현한 내용
3. 변경한 주요 파일
4. 실행한 명령과 실제 결과
5. 계약·DB 변경 여부
6. 남은 위험 또는 실행하지 못한 검증
7. 다음으로 가능한 작업 ID

## Code Review Rules

- 백엔드·프론트가 같은 계약을 따르는가
- 인증 없이 타인의 데이터에 접근 가능한가
- 재전송이 중복 저장되는가
- 100Hz 프레임을 JPA `save()` 반복으로 저장하는가
- WebSocket 실패가 원본 저장 실패를 일으키는가
- 분석 정렬에 `receivedAt`을 잘못 사용하는가
- 원본값을 보정값으로 덮어쓰는가
- 데이터 품질이 낮은데 정상처럼 표시하는가
- 의료 진단을 확정하는 문구가 있는가
- secret이나 개인정보가 코드·로그·fixture에 있는가
- 테스트가 실제 동작이 아니라 구현 세부사항만 따라가는가
