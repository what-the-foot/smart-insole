openapi: 3.0.3
info:
  title: Smart Insole API
  version: 1.1.0
  description: |
    스마트 인솔 MVP의 인증, 기기, 측정 세션, 센서 수신, 실시간 snapshot, 분석 결과 계약.

    1.1.0 변경 요약:
    - Frame Batch schemaVersion 1.1 (프레임별 receivedAt, protocolVersion, dataMode, IMU, flags, batchId)
    - 수신기용 세션 조회 API와 업로드 상태 보고 API (X-Receiver-Key)
    - ADC 상한 4095 (devices.adcMax, 세션 생성 시 스냅샷)
    - sampleRateHz 50/100, sourceType 요청 필드 (기본 DEVICE)
    - SensorPoint.label, 409 SESSION_NOT_MEASURING disposition
    - 분석 결과 rule-v1.2.0 관찰 단계(ObservationLevel)와 센서 share
servers:
- url: http://localhost:8080
  description: Local backend
tags:
- name: Auth
- name: Devices
- name: SensorLayouts
- name: Measurements
- name: Receiver
paths:
  /api/v1/auth/signup:
    post:
      tags:
      - Auth
      summary: 회원가입
      operationId: signup
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignupRequest'
      responses:
        '201':
          description: 가입 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSummary'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/auth/signin:
    post:
      tags:
      - Auth
      summary: 로그인
      operationId: signin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SigninRequest'
      responses:
        '200':
          description: 로그인 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthTokenResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/devices:
    get:
      tags:
      - Devices
      summary: 내 기기 목록
      operationId: listMyDevices
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 기기 목록
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DeviceResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - Devices
      summary: 기기 등록
      operationId: registerDevice
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDeviceRequest'
      responses:
        '201':
          description: 등록 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /api/v1/sensor-layouts/{version}:
    get:
      tags:
      - SensorLayouts
      summary: 센서 배치 조회
      operationId: getSensorLayout
      security:
      - bearerAuth: []
      parameters:
      - name: version
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: 센서 배치
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SensorLayoutResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/measurement-sessions:
    post:
      tags:
      - Measurements
      summary: 측정 세션 생성
      operationId: createMeasurementSession
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMeasurementSessionRequest'
      responses:
        '201':
          description: 생성 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
    get:
      tags:
      - Measurements
      summary: 내 측정 기록
      operationId: listMeasurementSessions
      security:
      - bearerAuth: []
      parameters:
      - name: page
        in: query
        schema:
          type: integer
          minimum: 0
          default: 0
      - name: size
        in: query
        schema:
          type: integer
          minimum: 1
          maximum: 100
          default: 20
      - name: status
        in: query
        schema:
          $ref: '#/components/schemas/MeasurementStatus'
      - name: from
        in: query
        description: Include sessions created at or after this UTC instant.
        schema:
          type: string
          format: date-time
      - name: to
        in: query
        description: Include sessions created at or before this UTC instant.
        schema:
          type: string
          format: date-time
      - name: minQualityScore
        in: query
        description: Include completed sessions whose data quality score is at least this value.
        schema:
          type: integer
          minimum: 0
          maximum: 100
      - name: patternCode
        in: query
        description: Include sessions containing this observed pattern code.
        schema:
          type: string
          maxLength: 100
      responses:
        '200':
          description: 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionPage'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /api/v1/measurement-sessions/{sessionId}:
    get:
      tags:
      - Measurements
      summary: 측정 상세
      operationId: getMeasurementSession
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 상세
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/v1/measurement-sessions/{sessionId}/start:
    post:
      tags:
      - Measurements
      summary: 측정 시작
      operationId: startMeasurementSession
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 처리 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/measurement-sessions/{sessionId}/complete:
    post:
      tags:
      - Measurements
      summary: 측정 종료 및 분석 예약
      operationId: completeMeasurementSession
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 처리 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/measurement-sessions/{sessionId}/cancel:
    post:
      tags:
      - Measurements
      summary: 측정 취소
      operationId: cancelMeasurementSession
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 처리 성공
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeasurementSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/measurement-sessions/{sessionId}/realtime-snapshot:
    get:
      tags:
      - Measurements
      summary: 최신 실시간 상태
      operationId: getRealtimeSnapshot
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: Snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RealtimePressureMessage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/measurement-sessions/{sessionId}/result:
    get:
      tags:
      - Measurements
      summary: 분석 결과
      operationId: getMeasurementResult
      security:
      - bearerAuth: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 완료 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalysisResultResponse'
        '202':
          description: 분석 중
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalysisPendingResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/recommendations/{code}:
    get:
      tags:
      - Recommendations
      summary: 추천 운동 가이드 상세
      operationId: getRecommendation
      security:
      - bearerAuth: []
      parameters:
      - name: code
        in: path
        required: true
        schema:
          type: string
          maxLength: 100
      responses:
        '200':
          description: 운동 가이드
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecommendationDetailResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /internal/v1/measurement-sessions/{sessionId}/frame-batches:
    post:
      tags:
      - Receiver
      summary: 센서 프레임 배치 업로드
      operationId: ingestFrameBatch
      security:
      - receiverApiKey: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FrameBatchRequest'
      responses:
        '200':
          description: 처리 결과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FrameBatchResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: |
            세션이 MEASURING이 아닙니다. `details.currentStatus`와 `details.disposition`을 포함합니다.
            `disposition`은 세션이 CREATED이면 `RETRY`(Retry-After 초 뒤 재시도), 그 외에는 `DROP`(배치 폐기)입니다.
          headers:
            X-Batch-Disposition:
              description: details.disposition과 같은 값(RETRY 또는 DROP). 보조 정보이며 본문이 1차 판별자입니다.
              schema:
                type: string
                enum:
                - RETRY
                - DROP
            Retry-After:
              description: disposition이 RETRY일 때만 포함되는 재시도 대기 초.
              schema:
                type: integer
                minimum: 1
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '413':
          description: 배치 크기 초과
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiError'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /internal/v1/measurement-sessions:
    get:
      tags:
      - Receiver
      summary: 수신기용 측정 중 세션 목록
      description: status는 MEASURING만 허용합니다. deviceSerial을 주면 좌우 기기 중 하나의 serialNumber가 일치하는 세션만 반환합니다. 빈 items 배열을 허용합니다.
      operationId: listReceiverSessions
      security:
      - receiverApiKey: []
      parameters:
      - name: status
        in: query
        required: true
        schema:
          type: string
          enum:
          - MEASURING
      - name: deviceSerial
        in: query
        required: false
        schema:
          type: string
          minLength: 1
          maxLength: 100
      responses:
        '200':
          description: 세션 목록
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReceiverSessionListResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /internal/v1/measurement-sessions/{sessionId}:
    get:
      tags:
      - Receiver
      summary: 수신기용 세션 조회
      description: 수신기가 세션에 배정된 기기 식별자·센서 수·adcMax·sampleRateHz를 확정하고 상태 전이를 폴링하는 데 사용합니다.
      operationId: getReceiverSession
      security:
      - receiverApiKey: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      responses:
        '200':
          description: 세션 정보
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReceiverSessionResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /internal/v1/measurement-sessions/{sessionId}/receiver-status:
    post:
      tags:
      - Receiver
      summary: 수신기 업로드 상태 보고
      description: MEASURING 세션에서만 허용됩니다. best-effort 보고이며 수신기는 실패해도 재시도하지 않습니다.
      operationId: reportReceiverStatus
      security:
      - receiverApiKey: []
      parameters:
      - $ref: '#/components/parameters/SessionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReceiverStatusRequest'
      responses:
        '204':
          description: 반영 성공
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /internal/v1/devices/{deviceId}/heartbeat:
    post:
      tags:
      - Receiver
      summary: 기기 상태 갱신
      operationId: heartbeatDevice
      security:
      - receiverApiKey: []
      parameters:
      - name: deviceId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceHeartbeatRequest'
      responses:
        '204':
          description: 반영 성공
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    receiverApiKey:
      type: apiKey
      in: header
      name: X-Receiver-Key
  parameters:
    SessionId:
      name: sessionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    BadRequest:
      description: 요청 형식 오류
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
    Unauthorized:
      description: 인증 실패
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
    Forbidden:
      description: 접근 권한 없음
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
    NotFound:
      description: 자원 없음
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
    Conflict:
      description: 현재 상태와 요청이 충돌함
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
    UnprocessableEntity:
      description: 의미 검증 실패
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiError'
  schemas:
    FootSide:
      type: string
      enum:
      - LEFT
      - RIGHT
    MeasurementStatus:
      type: string
      enum:
      - CREATED
      - MEASURING
      - PROCESSING
      - COMPLETED
      - CANCELLED
      - FAILED
    SourceType:
      type: string
      description: 자동 판별하지 않습니다. 기본값은 DEVICE이며 시뮬레이터(mock receiver, e2e 스크립트, 개발용 UI)만 SIMULATED를 명시합니다.
      enum:
      - DEVICE
      - SIMULATED
    DataMode:
      type: string
      description: 프레임의 센서 값 처리 모드. MVP BLE 와이어는 RAW 전용입니다.
      enum:
      - RAW
      - FILTERED
    ReceiverUploadState:
      type: string
      enum:
      - STREAMING
      - UPLOADING
      - UPLOAD_COMPLETE
    ObservationLevel:
      type: string
      description: 유효 걸음(접촉 구간) 창 기준 발생 비율로 정한 관찰 단계. 질환 판단이 아닙니다.
      enum:
      - NOT_OBSERVED
      - PARTIALLY_OBSERVED
      - REPEATEDLY_OBSERVED
    DeviceStatus:
      type: string
      enum:
      - ACTIVE
      - INACTIVE
      - DISCONNECTED
      - CALIBRATION_REQUIRED
    QualityLevel:
      type: string
      enum:
      - GOOD
      - ACCEPTABLE
      - POOR
    PatternSeverity:
      type: string
      enum:
      - INFO
      - CAUTION
      - RECHECK
    ContactState:
      type: string
      enum:
      - NO_CONTACT
      - CONTACT
      - UNKNOWN
    ApiError:
      type: object
      additionalProperties: false
      required:
      - code
      - message
      - details
      - traceId
      - timestamp
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
        traceId:
          type: string
        timestamp:
          type: string
          format: date-time
    SignupRequest:
      type: object
      additionalProperties: false
      required:
      - email
      - password
      - name
      properties:
        email:
          type: string
          format: email
          pattern: '.*\S.*'
          maxLength: 255
        password:
          type: string
          minLength: 8
          pattern: '.*\S.*'
          maxLength: 100
          description: Must be at most 72 bytes when encoded as UTF-8.
          x-maxUtf8Bytes: 72
        name:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 50
    SigninRequest:
      type: object
      additionalProperties: false
      required:
      - email
      - password
      properties:
        email:
          type: string
          format: email
          pattern: '.*\S.*'
          maxLength: 255
        password:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 100
          description: Must be at most 72 bytes when encoded as UTF-8.
          x-maxUtf8Bytes: 72
    UserSummary:
      type: object
      additionalProperties: false
      required:
      - userId
      - email
      - name
      - createdAt
      properties:
        userId:
          type: string
          format: uuid
        email:
          type: string
          format: email
        name:
          type: string
        createdAt:
          type: string
          format: date-time
    AuthTokenResponse:
      type: object
      additionalProperties: false
      required:
      - tokenType
      - accessToken
      - expiresInSeconds
      - user
      properties:
        tokenType:
          type: string
          enum:
          - Bearer
        accessToken:
          type: string
        expiresInSeconds:
          type: integer
          minimum: 1
        user:
          $ref: '#/components/schemas/UserSummary'
    RegisterDeviceRequest:
      type: object
      additionalProperties: false
      required:
      - serialNumber
      - displayName
      - footSide
      - sensorCount
      - sensorLayoutVersion
      - firmwareVersion
      properties:
        serialNumber:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 100
        displayName:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 100
        footSide:
          $ref: '#/components/schemas/FootSide'
        sensorCount:
          type: integer
          enum:
          - 6
          - 8
        sensorLayoutVersion:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 50
        firmwareVersion:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 50
        adcMax:
          type: integer
          description: 기기의 ADC 최댓값. 현재 허용값은 4095뿐이며 생략하면 4095입니다.
          enum:
          - 4095
          default: 4095
    DeviceResponse:
      type: object
      additionalProperties: false
      required:
      - deviceId
      - serialNumber
      - displayName
      - footSide
      - sensorCount
      - sensorLayoutVersion
      - firmwareVersion
      - adcMax
      - activeCalibrationVersion
      - status
      - registeredAt
      properties:
        deviceId:
          type: string
          format: uuid
        serialNumber:
          type: string
        displayName:
          type: string
        footSide:
          $ref: '#/components/schemas/FootSide'
        sensorCount:
          type: integer
          enum:
          - 6
          - 8
        sensorLayoutVersion:
          type: string
        firmwareVersion:
          type: string
        adcMax:
          type: integer
          minimum: 1
          description: 기기 등록 시 확정한 ADC 최댓값. V5 이전에 등록된 레거시 기기는 65535로 백필됩니다.
        activeCalibrationVersion:
          type: string
          nullable: true
        status:
          $ref: '#/components/schemas/DeviceStatus'
        lastSeenAt:
          type: string
          format: date-time
          nullable: true
        lastBatteryPercent:
          type: number
          minimum: 0
          maximum: 100
          nullable: true
          description: 마지막 heartbeat의 배터리 퍼센트. 미보정(255)은 수신기가 null로 보냅니다.
        lastBatteryMv:
          type: integer
          minimum: 0
          nullable: true
        registeredAt:
          type: string
          format: date-time
    SensorPoint:
      type: object
      additionalProperties: false
      required:
      - index
      - x
      - y
      - region
      - medialLateral
      properties:
        label:
          type: string
          nullable: true
          maxLength: 16
          description: 펌웨어 센서 라벨(S01..S08). index = S번호 - 1 = MUX 채널. 레거시 레이아웃은 null.
        index:
          type: integer
          minimum: 0
          maximum: 7
        x:
          type: number
          minimum: 0
          maximum: 1
          description: Foot-local horizontal coordinate from medial (0) to lateral (1). The UI mirrors LEFT-foot coordinates for a bilateral wearer-facing view.
        y:
          type: number
          minimum: 0
          maximum: 1
          description: Foot-local vertical coordinate from toe (0) to heel (1).
        region:
          type: string
          enum:
          - HEEL
          - MIDFOOT
          - FOREFOOT
          - TOE
        medialLateral:
          type: string
          enum:
          - MEDIAL
          - CENTER
          - LATERAL
    SensorLayoutResponse:
      type: object
      additionalProperties: false
      oneOf:
      - properties:
          sensorCount:
            enum:
            - 6
          points:
            minItems: 6
            maxItems: 6
      - properties:
          sensorCount:
            enum:
            - 8
          points:
            minItems: 8
            maxItems: 8
      required:
      - version
      - sensorCount
      - points
      properties:
        version:
          type: string
        sensorCount:
          type: integer
          enum:
          - 6
          - 8
        points:
          type: array
          description: Ordered sensor points. Length must equal sensorCount.
          oneOf:
          - minItems: 6
            maxItems: 6
          - minItems: 8
            maxItems: 8
          items:
            $ref: '#/components/schemas/SensorPoint'
    CreateMeasurementSessionRequest:
      type: object
      additionalProperties: false
      required:
      - leftDeviceId
      - rightDeviceId
      - sampleRateHz
      properties:
        leftDeviceId:
          type: string
          format: uuid
        rightDeviceId:
          type: string
          format: uuid
        sampleRateHz:
          type: integer
          description: 전송률의 단일 출처. BLE 전송 50Hz(측정 100Hz 분주) 또는 100Hz. UI 기본값은 50.
          enum:
          - 50
          - 100
        sourceType:
          allOf:
          - $ref: '#/components/schemas/SourceType'
          default: DEVICE
          description: 생략하면 DEVICE. 시뮬레이터만 SIMULATED를 명시합니다.
        memo:
          type: string
          maxLength: 500
          nullable: true
    MeasurementSessionResponse:
      type: object
      additionalProperties: false
      required:
      - sessionId
      - status
      - leftDeviceId
      - rightDeviceId
      - sampleRateHz
      - sourceType
      - adcMax
      - createdAt
      properties:
        sessionId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/MeasurementStatus'
        leftDeviceId:
          type: string
          format: uuid
        rightDeviceId:
          type: string
          format: uuid
        sampleRateHz:
          type: integer
        sourceType:
          $ref: '#/components/schemas/SourceType'
        adcMax:
          type: integer
          minimum: 1
          description: 세션 생성 시 기기에서 스냅샷한 ADC 최댓값.
        memo:
          type: string
          nullable: true
        dataQualityScore:
          type: integer
          minimum: 0
          maximum: 100
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        endedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        receiverState:
          allOf:
          - $ref: '#/components/schemas/ReceiverUploadState'
          nullable: true
          description: 수신기가 마지막으로 보고한 업로드 상태. 보고 전이면 null.
        receiverPendingBatches:
          type: integer
          minimum: 0
          nullable: true
          description: 수신기가 마지막으로 보고한 미전송 배치 수.
    MeasurementSessionPage:
      type: object
      additionalProperties: false
      required:
      - items
      - page
      - size
      - totalElements
      - totalPages
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MeasurementHistoryItem'
        page:
          type: integer
          minimum: 0
        size:
          type: integer
          minimum: 1
        totalElements:
          type: integer
          minimum: 0
        totalPages:
          type: integer
          minimum: 0
    MeasurementHistoryItem:
      type: object
      additionalProperties: false
      required:
      - sessionId
      - status
      - leftDeviceId
      - rightDeviceId
      - sampleRateHz
      - sourceType
      - createdAt
      - primaryPatternCode
      properties:
        sessionId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/MeasurementStatus'
        leftDeviceId:
          type: string
          format: uuid
        rightDeviceId:
          type: string
          format: uuid
        sampleRateHz:
          type: integer
        sourceType:
          $ref: '#/components/schemas/SourceType'
        memo:
          type: string
          nullable: true
        dataQualityScore:
          type: integer
          minimum: 0
          maximum: 100
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        endedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        primaryPatternCode:
          type: string
          nullable: true
    ImuVector:
      type: array
      description: int16 3축 벡터 (x, y, z).
      minItems: 3
      maxItems: 3
      items:
        type: integer
        minimum: -32768
        maximum: 32767
    PressureFrameInput:
      type: object
      additionalProperties: false
      description: |
        schemaVersion 1.0 프레임은 필수 5개 필드만 허용합니다. 1.1 선택 필드를 1.0 배치에 넣으면
        해당 프레임은 `SCHEMA_FIELD_NOT_ALLOWED`로 거부됩니다. 값이 null인 키는 생략합니다.
      required:
      - deviceId
      - footSide
      - sequence
      - deviceTimeMs
      - sensorValues
      properties:
        deviceId:
          type: string
          format: uuid
        footSide:
          $ref: '#/components/schemas/FootSide'
        sequence:
          type: integer
          format: int64
          minimum: 0
          maximum: 4294967295
          description: 단조 증가 u32. 수신기가 v1 u16을 펼쳐 보냅니다.
        deviceTimeMs:
          type: integer
          format: int64
          minimum: 0
        sensorValues:
          type: array
          description: RAW ADC 값. 상한은 세션 adcMax(현재 4095)이며 4095는 포화를 뜻합니다.
          oneOf:
          - minItems: 6
            maxItems: 6
          - minItems: 8
            maxItems: 8
          items:
            type: integer
            minimum: 0
            maximum: 4095
        protocolVersion:
          type: integer
          minimum: 1
          description: '(1.1) BLE 패킷 프로토콜 버전. 1 = Frame A/B, 2 = 단일 패킷.'
        receivedAt:
          type: string
          format: date-time
          description: '(1.1) 프레임별 수신기(PC) 수신 시각. 좌우 정렬 기준.'
        dataMode:
          allOf:
          - $ref: '#/components/schemas/DataMode'
          description: '(1.1) v2에서는 RAW 고정.'
        calibrated:
          type: boolean
          description: '(1.1) 기기 측 영점 보정 여부. v2에서는 false 고정.'
        imuAvailable:
          type: boolean
          description: '(1.1) IMU 값 유효 여부. flags의 IMU_ERROR(bit1)가 켜지면 백엔드가 false로 정규화합니다.'
        accelMg:
          allOf:
          - $ref: '#/components/schemas/ImuVector'
          description: '(1.1) 가속도 mg.'
        gyroDps10:
          allOf:
          - $ref: '#/components/schemas/ImuVector'
          description: '(1.1) 각속도 0.1 °/s.'
        flags:
          type: integer
          minimum: 0
          maximum: 255
          description: '(1.1, protocolVersion 2 이상) bit0 FSR_ERROR, bit1 IMU_ERROR, bit2 BATTERY_LOW, bit3..7 예약 0.'
    FrameBatchRequest:
      type: object
      additionalProperties: false
      required:
      - schemaVersion
      - receiverId
      - sentAt
      - frames
      properties:
        schemaVersion:
          type: string
          enum:
          - '1.0'
          - '1.1'
        receiverId:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 100
        batchId:
          type: string
          minLength: 1
          maxLength: 100
          description: '(1.1) 수신기 Outbox 배치 식별자. 멱등성은 여전히 (session, device, sequence) 기준입니다.'
        sentAt:
          type: string
          format: date-time
        frames:
          type: array
          minItems: 1
          maxItems: 200
          items:
            $ref: '#/components/schemas/PressureFrameInput'
    FrameRejection:
      type: object
      additionalProperties: false
      required:
      - frameIndex
      - code
      - message
      properties:
        frameIndex:
          type: integer
          minimum: 0
        code:
          type: string
        message:
          type: string
    FrameBatchResponse:
      type: object
      additionalProperties: false
      required:
      - acceptedCount
      - duplicateCount
      - rejectedCount
      - rejections
      - lastSequenceByDevice
      - receivedAt
      properties:
        acceptedCount:
          type: integer
          minimum: 0
        duplicateCount:
          type: integer
          minimum: 0
        rejectedCount:
          type: integer
          minimum: 0
        rejections:
          type: array
          items:
            $ref: '#/components/schemas/FrameRejection'
        lastSequenceByDevice:
          type: object
          additionalProperties:
            type: integer
            format: int64
            minimum: 0
        receivedAt:
          type: string
          format: date-time
    DeviceHeartbeatRequest:
      type: object
      additionalProperties: false
      required:
      - receiverId
      - observedAt
      - connected
      properties:
        receiverId:
          type: string
          minLength: 1
          pattern: '.*\S.*'
        observedAt:
          type: string
          format: date-time
        connected:
          type: boolean
        batteryPercent:
          type: number
          minimum: 0
          maximum: 100
          nullable: true
          description: Status battery_pct. 255(미보정/미측정)는 수신기가 null로 보냅니다.
        batteryMv:
          type: integer
          minimum: 0
          nullable: true
          description: Status battery_mv.
        firmwareVersion:
          type: string
          minLength: 1
          maxLength: 50
          nullable: true
          description: Status fw_major.fw_minor.fw_patch (예 0.2.0). 기기 firmwareVersion을 갱신합니다.
        rssi:
          type: integer
          nullable: true
    ReceiverSessionDevice:
      type: object
      additionalProperties: false
      required:
      - deviceId
      - serialNumber
      - footSide
      - sensorCount
      - sensorLayoutVersion
      - adcMax
      - firmwareVersion
      properties:
        deviceId:
          type: string
          format: uuid
        serialNumber:
          type: string
          description: 'SMART-INSOLE-{L|R}-{serial u32 hex 8자리} 규칙 권장. 수신기는 스캔 결과와 대조합니다.'
        footSide:
          $ref: '#/components/schemas/FootSide'
        sensorCount:
          type: integer
          enum:
          - 6
          - 8
        sensorLayoutVersion:
          type: string
        adcMax:
          type: integer
          minimum: 1
        firmwareVersion:
          type: string
          nullable: true
    ReceiverSessionResponse:
      type: object
      additionalProperties: false
      required:
      - sessionId
      - status
      - sampleRateHz
      - sourceType
      - adcMax
      - startedAt
      - endedAt
      - left
      - right
      - receiverState
      - receiverPendingBatches
      properties:
        sessionId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/MeasurementStatus'
        sampleRateHz:
          type: integer
          enum:
          - 50
          - 100
        sourceType:
          $ref: '#/components/schemas/SourceType'
        adcMax:
          type: integer
          minimum: 1
          description: 세션 adcMax 스냅샷. left/right.adcMax와 같습니다.
        startedAt:
          type: string
          format: date-time
          nullable: true
        endedAt:
          type: string
          format: date-time
          nullable: true
        left:
          $ref: '#/components/schemas/ReceiverSessionDevice'
        right:
          $ref: '#/components/schemas/ReceiverSessionDevice'
        receiverState:
          allOf:
          - $ref: '#/components/schemas/ReceiverUploadState'
          nullable: true
        receiverPendingBatches:
          type: integer
          minimum: 0
          nullable: true
    ReceiverSessionListResponse:
      type: object
      additionalProperties: false
      required:
      - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ReceiverSessionResponse'
    ReceiverStatusRequest:
      type: object
      additionalProperties: false
      required:
      - receiverId
      - state
      - pendingBatchCount
      - observedAt
      properties:
        receiverId:
          type: string
          minLength: 1
          pattern: '.*\S.*'
          maxLength: 100
        state:
          $ref: '#/components/schemas/ReceiverUploadState'
        pendingBatchCount:
          type: integer
          minimum: 0
        observedAt:
          type: string
          format: date-time
    CopPoint:
      type: object
      additionalProperties: false
      required:
      - x
      - y
      properties:
        x:
          type: number
          minimum: 0
          maximum: 1
        y:
          type: number
          minimum: 0
          maximum: 1
    FootRealtimeData:
      type: object
      nullable: true
      additionalProperties: false
      required:
      - connected
      - lastSequence
      - deviceTimeMs
      - sensorValues
      - totalPressure
      - cop
      - contactState
      - lastReceivedAt
      properties:
        connected:
          type: boolean
        lastSequence:
          type: integer
          format: int64
          minimum: 0
        deviceTimeMs:
          type: integer
          format: int64
          minimum: 0
        sensorValues:
          type: array
          oneOf:
          - minItems: 6
            maxItems: 6
          - minItems: 8
            maxItems: 8
          items:
            type: number
            minimum: 0
            maximum: 100
        totalPressure:
          type: number
          minimum: 0
        cop:
          allOf:
          - $ref: '#/components/schemas/CopPoint'
          nullable: true
        contactState:
          $ref: '#/components/schemas/ContactState'
        lastReceivedAt:
          type: string
          format: date-time
    RealtimeQuality:
      type: object
      additionalProperties: false
      required:
      - score
      - level
      - flags
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        level:
          $ref: '#/components/schemas/QualityLevel'
        flags:
          type: array
          uniqueItems: true
          items:
            type: string
    RealtimePressureMessage:
      type: object
      additionalProperties: false
      required:
      - schemaVersion
      - sessionId
      - serverTime
      - elapsedTimeMs
      - status
      - left
      - right
      - quality
      properties:
        schemaVersion:
          type: string
          enum:
          - '1.0'
        sessionId:
          type: string
          format: uuid
        serverTime:
          type: string
          format: date-time
        elapsedTimeMs:
          type: integer
          minimum: 0
        status:
          type: string
          enum:
          - MEASURING
        left:
          nullable: true
          allOf:
          - $ref: '#/components/schemas/FootRealtimeData'
        right:
          nullable: true
          allOf:
          - $ref: '#/components/schemas/FootRealtimeData'
        quality:
          $ref: '#/components/schemas/RealtimeQuality'
    AnalysisPendingResponse:
      type: object
      additionalProperties: false
      required:
      - sessionId
      - status
      - message
      properties:
        sessionId:
          type: string
          format: uuid
        status:
          type: string
          enum:
          - PROCESSING
        message:
          type: string
    DataQualityResult:
      type: object
      additionalProperties: false
      required:
      - score
      - level
      - missingFrameRate
      - flags
      properties:
        score:
          type: integer
          minimum: 0
          maximum: 100
        level:
          $ref: '#/components/schemas/QualityLevel'
        missingFrameRate:
          type: number
          minimum: 0
          maximum: 1
        flags:
          type: array
          description: |
            품질 플래그. 예: SEQUENCE_GAP, OUT_OF_ORDER, DEVICE_TIME_JUMP, SENSOR_STUCK_OR_SATURATED,
            LEFT/RIGHT_DATA_MISSING, LEFT/RIGHT_DATA_INCOMPLETE, LEFT/RIGHT_DEVICE_DISCONNECTED, INSUFFICIENT_DATA,
            SEQUENCE_WRAP_SUSPECTED, SAMPLE_RATE_MISMATCH, RECEIVER_UPLOAD_INCOMPLETE,
            FSR_ERROR_REPORTED, IMU_ERROR_REPORTED, BATTERY_LOW_REPORTED, FILTERED_DATA_MODE, LOW_DATA_QUALITY.
          items:
            type: string
    GaitSummary:
      type: object
      additionalProperties: false
      required:
      - validStepCount
      - cadence
      - leftContactTimeMs
      - rightContactTimeMs
      - symmetryIndex
      properties:
        validStepCount:
          type: integer
          nullable: true
          minimum: 0
          description: Number of valid contact events used by the analysis; null for legacy results.
        cadence:
          type: number
          minimum: 0
        leftContactTimeMs:
          type: number
          minimum: 0
        rightContactTimeMs:
          type: number
          minimum: 0
        symmetryIndex:
          type: number
          minimum: 0
    PressureDistribution:
      type: object
      additionalProperties: false
      required:
      - leftMedialRatio
      - leftLateralRatio
      - rightMedialRatio
      - rightLateralRatio
      - leftHeelRatio
      - rightHeelRatio
      - leftMidfootRatio
      - rightMidfootRatio
      - leftForefootRatio
      - rightForefootRatio
      - leftPeakPressure
      - rightPeakPressure
      - leftMeanCoP
      - rightMeanCoP
      properties:
        leftMedialRatio:
          type: number
          minimum: 0
          maximum: 1
        leftLateralRatio:
          type: number
          minimum: 0
          maximum: 1
        rightMedialRatio:
          type: number
          minimum: 0
          maximum: 1
        rightLateralRatio:
          type: number
          minimum: 0
          maximum: 1
        leftHeelRatio:
          type: number
          minimum: 0
          maximum: 1
        rightHeelRatio:
          type: number
          minimum: 0
          maximum: 1
        leftMidfootRatio:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
        rightMidfootRatio:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
        leftForefootRatio:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
          description: Combined FOREFOOT and TOE pressure ratio for the left foot.
        rightForefootRatio:
          type: number
          nullable: true
          minimum: 0
          maximum: 1
          description: Combined FOREFOOT and TOE pressure ratio for the right foot.
        leftPeakPressure:
          type: number
          nullable: true
          minimum: 0
          maximum: 100
          description: Maximum normalized left sensor value observed during the session.
        rightPeakPressure:
          type: number
          nullable: true
          minimum: 0
          maximum: 100
          description: Maximum normalized right sensor value observed during the session.
        leftMeanCoP:
          nullable: true
          description: Pressure-weighted mean left center of pressure, or null when unavailable.
          allOf:
          - $ref: '#/components/schemas/CopPoint'
        rightMeanCoP:
          nullable: true
          description: Pressure-weighted mean right center of pressure, or null when unavailable.
          allOf:
          - $ref: '#/components/schemas/CopPoint'
        leftSensorSharePct:
          nullable: true
          description: '(rule-v1.2.0) 접촉 프레임 평균의 센서별 share(센서/전체합×100), 레이아웃 index 순, 합 100. 접촉 프레임이 없거나 이전 결과면 null.'
          allOf:
          - $ref: '#/components/schemas/SensorSharePct'
        rightSensorSharePct:
          nullable: true
          description: '(rule-v1.2.0) 오른발 센서별 share. 접촉 프레임이 없거나 이전 결과면 null.'
          allOf:
          - $ref: '#/components/schemas/SensorSharePct'
    SensorSharePct:
      type: array
      oneOf:
      - minItems: 6
        maxItems: 6
      - minItems: 8
        maxItems: 8
      items:
        type: number
        minimum: 0
        maximum: 100
    PatternResult:
      type: object
      additionalProperties: false
      description: |
        rule-v1.2.0 패턴 코드는 MEDIAL_LOAD_TENDENCY, LATERAL_LOAD_TENDENCY, LEFT_RIGHT_ASYMMETRY,
        LOW_HALLUX_SIGNAL, FOREFOOT_LOAD_TENDENCY, REARFOOT_LOAD_TENDENCY 6종의 부분집합이며
        PARTIALLY_OBSERVED 또는 REPEATEDLY_OBSERVED만 patterns에 포함됩니다.
      required:
      - code
      - severity
      - title
      - message
      - evidence
      properties:
        code:
          type: string
        severity:
          $ref: '#/components/schemas/PatternSeverity'
        title:
          type: string
        message:
          type: string
        evidence:
          type: string
        observationLevel:
          allOf:
          - $ref: '#/components/schemas/ObservationLevel'
          nullable: true
          description: rule-v1.2.0 이상. 이전 결과는 null.
        occurrenceRate:
          type: number
          minimum: 0
          maximum: 1
          nullable: true
          description: observedCount / windowCount. 이전 결과는 null.
        observedCount:
          type: integer
          minimum: 0
          nullable: true
        windowCount:
          type: integer
          minimum: 0
          nullable: true
          description: 판정에 사용한 창(유효 걸음) 수. LEFT_RIGHT_ASYMMETRY는 좌우 걸음 쌍 수.
    ObservationSummaryItem:
      type: object
      additionalProperties: false
      required:
      - code
      - observationLevel
      - occurrenceRate
      - observedCount
      - windowCount
      properties:
        code:
          type: string
          enum:
          - MEDIAL_LOAD_TENDENCY
          - LATERAL_LOAD_TENDENCY
          - LEFT_RIGHT_ASYMMETRY
          - LOW_HALLUX_SIGNAL
          - FOREFOOT_LOAD_TENDENCY
          - REARFOOT_LOAD_TENDENCY
        observationLevel:
          $ref: '#/components/schemas/ObservationLevel'
        occurrenceRate:
          type: number
          minimum: 0
          maximum: 1
        observedCount:
          type: integer
          minimum: 0
        windowCount:
          type: integer
          minimum: 0
    RecommendationSummary:
      type: object
      additionalProperties: false
      required:
      - code
      - title
      - summary
      - durationMinutes
      properties:
        code:
          type: string
        title:
          type: string
        summary:
          type: string
        durationMinutes:
          type: integer
          minimum: 1
    RecommendationDetailResponse:
      type: object
      additionalProperties: false
      required:
      - code
      - title
      - purpose
      - instructions
      - durationMinutes
      - cautionText
      - relatedPatternCodes
      properties:
        code:
          type: string
        title:
          type: string
        purpose:
          type: string
        instructions:
          type: array
          minItems: 1
          items:
            type: string
        durationMinutes:
          type: integer
          minimum: 1
        cautionText:
          type: string
        relatedPatternCodes:
          type: array
          items:
            type: string
    AnalysisResultResponse:
      type: object
      additionalProperties: false
      required:
      - sessionId
      - status
      - algorithmVersion
      - dataQuality
      - gaitSummary
      - pressureDistribution
      - patterns
      - recommendations
      - disclaimer
      - createdAt
      properties:
        sessionId:
          type: string
          format: uuid
        status:
          type: string
          enum:
          - COMPLETED
        algorithmVersion:
          type: string
        dataQuality:
          $ref: '#/components/schemas/DataQualityResult'
        gaitSummary:
          $ref: '#/components/schemas/GaitSummary'
        pressureDistribution:
          $ref: '#/components/schemas/PressureDistribution'
        patterns:
          type: array
          items:
            $ref: '#/components/schemas/PatternResult'
        observationSummary:
          type: array
          nullable: true
          description: rule-v1.2.0 이상에서 6종 코드 전체의 관찰 단계. 이전 알고리즘 결과는 null.
          items:
            $ref: '#/components/schemas/ObservationSummaryItem'
        recommendations:
          type: array
          items:
            $ref: '#/components/schemas/RecommendationSummary'
        disclaimer:
          type: string
        createdAt:
          type: string
          format: date-time
